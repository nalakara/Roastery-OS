{
  "name": "roastery-os",
  "version": "0.1.0",
  "private": true,
  "description": "Roastery OS — Modular Specialty Coffee Production & Inventory Operating System",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "tsc -b tsconfig.json",
    "typecheck": "tsc --build tsconfig.json",
    "test": "npm run test --workspaces"
  },
  "dependencies": {
    "typescript": "^5.4.5"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  }
}
