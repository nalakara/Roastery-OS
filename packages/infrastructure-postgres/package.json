{
  "name": "@roastery-os/infrastructure-postgres",
  "version": "0.1.0",
  "private": true,
  "description": "Roastery OS — PostgreSQL Schema, Migrations, and Repositories",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "test": "node --test dist/**/*.test.js || true"
  },
  "dependencies": {
    "@roastery-os/contracts": "*",
    "@roastery-os/domain-core": "*",
    "pg": "^8.11.5"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "@types/pg": "^8.11.5",
    "pg-mem": "^3.0.14"
  }
}
