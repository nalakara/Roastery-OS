{
  "name": "@roastery-os/domain-core",
  "version": "0.1.0",
  "private": true,
  "description": "Roastery OS — Pure Domain Aggregates, Invariants, and Value Objects",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "test": "node --test dist/**/*.test.js"
  },
  "dependencies": {
    "@roastery-os/contracts": "*",
    "decimal.js": "^10.4.3"
  },
  "devDependencies": {
    "@types/node": "^20.12.7",
    "typescript": "^5.4.5"
  }
}
